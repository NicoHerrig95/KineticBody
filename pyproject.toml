[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "KineticBody"
version = "0.1.0"
description = "KineticBody project package"
authors = [
    { name = "Nico Herrig", email = "nico.herrig95@gmail.com" }
]
dependencies = []