[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "BodyScan"
version = "0.1.0"
description = "BodyScan project package"
authors = [
    { name = "Your Name", email = "your.email@example.com" }
]
dependencies = []